.text-center
  = form_tag(nil, class: "form-inline") do 
    .input-group{style: 'width:65%'}
      = collection_select(:school, :active_process_id, school.academic_processes, :id, :period_name, {include_blank: 'Sin Proceso Activo', selected: school.enroll_process_id}, {class: "form-control form-control-sm ", 'data-bs-original-title': "Esta acción modificará la posibilidad de calificar a los profesores, entre otras cosas. Para desactivar el período seleccione 'Sin Perído Activo'", onchange: 'setEnabledEnroll(this);', id: "enroll_process_id#{school.id}"})
