
- if (current_user&.admin&.authorized_read? 'AcademicProcess')
  - if EnrollmentDay.any?
    = link_to "/enrollment_days/#{-1}/destroy_all", class: 'btn btn-danger mx-2 float-end', title: 'Eliminar todas las citas horarias', data: {confirm: 'Esta acción eliminará todas las citas horarias registradas en COES ¿Está completamente seguro?'} do
      %i.fa-regular.fa-broom
      Limpiar Citas

  - if @object.process_before

    = link_to "/academic_processes/#{@object.process_before.id}/run_regulation", class: 'btn mb-3 btn-primary', title: "Actualiza de estado de permanecia de los estudiantes basado en los períodos anteriores a #{@object.period_name}. Incluye eliminación de Citas", data: {confirm: "Esta acción actualizará el Estado de Permanencia y los Promedios de Notas de cada estudiante cursado en el periodo anterior #{@object.process_before.period_name} ¿Está completamente seguro?"} do
      %i.fa-regular.fa-gavel
      Correr Reglamento

    - enrollment_days = @object.enrollment_days
    - grades_without_appointment = @object.readys_to_enrollment_day

    = render partial: "/enrollment_days/index", locals: {enrollment_days: enrollment_days, grades_without_appointment: grades_without_appointment, academic_process: @object}

  - else
    .alert.alert-warning= 'Sin proceso academico anterio vinculado. Para habilitar el sistema de Cita Horaria en este proceso académico, por favor edítelo y agregue un proceso anteriór. También es importante limpiar toda cita horaria anterior'
- else
  .text-center.alert.alert-warning Acceso Restringido
