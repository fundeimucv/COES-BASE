%table.table.table-bordered.table-sm.table-responsive.table-striped.table-hover
	%thead
		%tr
			%th
			%th.text-center{colspan: 5} Permisos
		%tr.text-center
			%th.text-center.col-10 Función
			%th Todos/Ninguno
			%th Crear			
			%th Ver Listas y Detalles
			%th Actualizar
			%th Eliminar

	%tbody
		= form_tag('#', class: 'form-horizontal', id: 'form_clonar') do

			- ActiveRecord::Base.connection.tables.map do |model|
				%tr.text-center
					%td.text-start
						= ApplicationController.helpers.translate_model model.singularize
					%td= check_box_tag "#{model}[can_crate]", nil
					%td= check_box_tag "#{model}[can_crate]", nil
					%td= check_box_tag "#{model}[can_read]", nil
					%td= check_box_tag "#{model}[can_update]", nil
					%td= check_box_tag "#{model}[can_delete]", nil


