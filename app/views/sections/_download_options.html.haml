- if ((section.qualified?) or (current_user.admin? and session[:rol] and session[:rol].eql? 'admin'))
  = link_to "/sections/#{section.id}.pdf", class: 'btn btn-success btn-sm float-right' do
    .fa.fa-download.fa-list
    Acta PDF  

= link_to "/sections/#{section.id}/export.xls", class: 'btn btn-info float-end m-3' do
  .fa.fa-download
  Descargar Listado